<div class="container">
  <div class="row">
    <div class="col-sm-12">
      <div class="row">
        <% @artists.each do |artist| %>
          <div class="col-sm-3">
            <div class="thumbnail">
              <img src="<%= artist.image_url %>" alt="<%= artist.name %>" width="300" height="300">
              <div class="caption">
                <h3 class="text-center"><%= artist.name %></h3>
                <%= link_to "Ver más", artist, class: "btn btn-primary btn-block" %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>